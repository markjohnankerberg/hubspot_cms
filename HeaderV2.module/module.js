/* Header V2 — the live-search JavaScript is inlined at the bottom of module.html
   to guarantee execution. HubSpot's module.js bundler is unreliable for header
   modules placed in global partials / page columns; an inline <script> in the
   HTML is the reliable delivery path. The menu / dropdown / submenu behaviour
   lives in the {% require_js %} block in module.html (jQuery). This file is
   intentionally empty. */
