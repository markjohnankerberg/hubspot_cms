/* ATRI Header v2 — JavaScript is inlined in module.html to guarantee execution.
   HubSpot's module.js bundler is unreliable for modules placed in page columns;
   inline <script> in the HTML is the reliable delivery path in that context.
   This file is intentionally empty. */
